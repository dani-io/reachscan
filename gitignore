# Build artifacts
/dist/
scanner
scanner.exe

# Generated data (re-generate with fetch_ranges.sh + make_targets.py)
prefixes.txt
hetzner-prefixes.txt
ovh-prefixes.txt

# Scan results (contain public IP — keep local)
results-*.json
reports/
whitelist-universal.txt
blocked-by-iran.txt

# Go
*.test
*.out
go.sum
vendor/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS
Thumbs.db
desktop.ini
